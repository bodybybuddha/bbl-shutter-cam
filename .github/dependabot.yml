version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    # Dependabot for pip primarily tracks requirements files.
    # This repo also declares dependencies in pyproject.toml; keep those in sync as part of reviews.
